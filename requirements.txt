# Pinned to versions verified against the venv used during development.
# Bump deliberately; do not let `pip install -U` silently change majors.
secops==0.41.0
google-cloud-monitoring==2.30.0
google-genai==1.72.0
PyYAML==6.0.3
requests==2.33.1
functions-framework==3.10.1
